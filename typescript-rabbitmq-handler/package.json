{
  "name": "typescript-rabbitmq-handler",
  "version": "0.0.5",
  "description": "A utility for handling RabbitMQ connections and interactions.",
  "main": "dist/index.js",
  "types": "dist/index.d.js",
  "author": "Holovanets Viktor",
  "private": false,
  "license": "ISC",
  "files": [
    "/dist"
  ],
  "dependencies": {
    "amqplib": "^0.10.3",
    "ts-node": "^10.9.1",
    "typescript": "^5.1.3"
  },
  "scripts": {
    "build": "mkdir -p dist && microbundle --tsconfig tsconfig.json --no-sourcemap"
  },
  "keywords": [
    "rabbitmq",
    "amqplib",
    "messaging"
  ],
  "devDependencies": {
    "@types/amqplib": "^0.10.1",
    "@types/node": "^20.5.2",
    "microbundle": "^0.15.1"
  }
}
